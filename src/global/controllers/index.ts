/**
 * Global Controllers — reusable controller helpers shared across modules.
 * Module-specific controllers live inside their respective module directories.
 */
export * from "../../modules/controllers/auth.controller";
